services:
  - type: web
    runtime: static
    name: secc-minforsac
    buildCommand: npm install && npm run build
    staticPublishPath: dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html